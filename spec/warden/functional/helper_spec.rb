require 'spec_helper'

describe Warden::Functional::Helper do
  it 'has a version number' do
    expect(Warden::Functional::Helper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
